<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/stylsheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block bg-zinc-800 rounded w-full outline-none px-5 py-2" type="text" placeholder="Title"  name="title">
                <textarea class="block bg-zinc-800 rounded w-full mt-2 resize-none px-5 py-2 outline-none"
                    placeholder="Write a note..." name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded" type="submit" value="Create Note">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length > 0){ %>
            <% files.forEach(function(val){ %>
                <div class="task bg-zinc-800 rounded min-w-72 px-3 py-4">
                    <h1 class="text-white tracking-tighter text-3xl"><%= val %> </h1>
                    <div class="flex mt-3 justify-between items-center">
                        <a class="text-blue-500 inline-block" href="/files/<%= val %>">Read More</a>
                        <a class="text-zinc-500" href="/edit/<%= val %>">edit filename</a>
                    </div>
                </div>
            <% }) %> 
            <% } else{ %>
                <h3 class="text-zinc-600">No notes yet.</h3>
            <% } %>
        </div>
    </div>
</body>

</html>